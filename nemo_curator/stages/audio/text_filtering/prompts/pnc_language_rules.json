{
  "as": "Assamese exact inserted glyphs — statement/command `।`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "bn": "Bengali exact inserted glyphs — statement/command `।`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "gu": "Gujarati exact inserted glyphs — statement/command `.`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "hi": "Hindi exact inserted glyphs — statement/command `।`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "kn": "Kannada exact inserted glyphs — statement/command `.`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "ml": "Malayalam exact inserted glyphs — statement/command `.`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "mr": "Marathi exact inserted glyphs — statement/command `.`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "or": "Odia exact inserted glyphs — statement/command `।`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "pa": "Punjabi in Gurmukhi exact inserted glyphs — statement/command `।`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "ta": "Tamil exact inserted glyphs — statement/command `.`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "te": "Telugu exact inserted glyphs — statement/command `.`; direct question `?`; exclamation `!`; comma `,`; semicolon `;`; colon `:`; ellipsis `…`. These are the only glyphs to insert.",
  "ur": "Urdu exact inserted glyphs — statement/command `۔`; direct question `؟`; exclamation `!`; comma `،`; semicolon `؛`; colon `:`; ellipsis `…`. These are the only glyphs to insert; preserve logical Unicode order."
}
